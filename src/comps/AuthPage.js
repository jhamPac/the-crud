import React from 'react'
import { firebaseRef } from '../firebase'

export default function AuthPage() {

  setTimeout(async () => {
    
  }, 3000)

  return (
    <div>This is the Authpage</div>
  )
}
